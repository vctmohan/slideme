<%= javascript_tag do %>
    var SLConfig = {
    <% if logged_in? %> "current_user":  <%= raw UserSerializer.new(current_user).to_json %>,<% end %>
    <% if @deck %>"deck": <%= raw DeckSerializer.new(@deck).to_json %><% end %>
    };
<% end %>